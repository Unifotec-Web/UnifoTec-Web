import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import PageHero from "@/components/PageHero";
import CTA from "@/components/CTA";
import { deliverySteps } from "@/lib/public-content";
export default function ProcessPage() { return <main className="min-h-screen pt-20"><Navbar /><PageHero eyebrow="How we work" title="A clear process for purposeful digital work" description="Responsibilities, decisions and approval points stay visible from the first conversation to launch." image="/images/photos/about-workshop.webp" imageAlt="Professionals discussing work around a table" action={{label:"Discuss a project",href:"/start-project"}} /><section className="bg-white py-16 sm:py-20"><div className="section-shell"><ol className="grid gap-4 md:grid-cols-2">{deliverySteps.map(([title, description], index) => <li key={title} className="flex gap-5 rounded-[1.5rem] border border-slate-200 bg-[#fbfaf7] p-6 sm:p-8"><span className="text-2xl font-black text-[#285749]">{String(index + 1).padStart(2,"0")}</span><div><h2 className="text-xl font-bold text-slate-900">{title}</h2><p className="mt-3 leading-relaxed text-slate-600">{description}</p></div></li>)}</ol></div></section><CTA /><Footer /></main>; }
