"use client";
import Image from "next/image"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import { company, services } from "@/lib/public-content";
export default function StartProjectPage() { const subject = encodeURIComponent("Project enquiry"); const body = encodeURIComponent("Hello UNIFOTEC-WEB,\n\nI would like to discuss a project."); return <main className="min-h-screen pt-20 bg-slate-50"><Navbar /><section className="py-24 max-w-2xl mx-auto px-4 text-center"><Image src="/images/illustrations/discovery.svg" alt="Illustration of a planned digital project journey." width={160} height={100} loading="lazy" className="mx-auto mb-6 h-auto w-40" /><h1 className="text-4xl font-black text-dark mb-5">Start a Project</h1><p className="text-grey mb-8">Tell us about your goals by email. Opening the draft does not send a message; review it and send it when ready.</p><div className="grid grid-cols-1 sm:grid-cols-2 gap-3 text-left mb-8">{services.map(([title]) => <div key={title} className="bg-white border border-gray-100 rounded-xl p-4 text-sm font-bold text-dark">{title}</div>)}</div><a href={`${company.emailHref}?subject=${subject}&body=${body}`} className="inline-block bg-primary text-white px-8 py-4 rounded-xl font-bold">Open Email Draft</a><p className="mt-5 text-sm text-grey">Or <a className="text-primary underline" href="/contact">contact us</a> for the same email-draft option.</p></section><Footer /></main>; }
