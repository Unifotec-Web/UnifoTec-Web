import Link from "next/link";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import PageHero from "@/components/PageHero";
import { company, deliverySteps } from "@/lib/public-content";
import { ArrowUpRight } from "lucide-react";
export default function StartProjectPage() { const subject = encodeURIComponent("Project enquiry"); const body = encodeURIComponent("Hello UNIFOTEC-WEB,\n\nI would like to discuss a project.\n\nMy goals are:\n\nMy preferred next step is:"); return <main className="min-h-screen pt-20"><Navbar /><PageHero eyebrow="Start a project" title="Let's shape your next digital move" description="Tell us what you want to achieve. A first conversation helps define the scope, people, and practical next steps." image="/images/photos/about-workshop.webp" imageAlt="Professionals discussing a project together" /><section className="bg-white py-16 sm:py-20"><div className="section-shell grid gap-10 lg:grid-cols-[1fr_0.9fr]"><div><p className="eyebrow mb-4">What happens next</p><h2 className="section-heading">Start with a clear brief.</h2><p className="mt-5 max-w-xl leading-relaxed text-slate-600">A useful first message can include your goals, audience, existing tools, and the timing you have in mind. We will discuss the details before any scope is agreed.</p><ol className="mt-8 space-y-4">{deliverySteps.slice(0,3).map(([title,description],index)=><li key={title} className="flex gap-4 border-t border-slate-200 pt-4"><span className="font-bold text-[#285749]">0{index+1}</span><span><strong className="block text-slate-900">{title}</strong><span className="text-sm text-slate-600">{description}</span></span></li>)}</ol></div><div className="self-start rounded-[1.75rem] bg-[#edf1eb] p-7 sm:p-10"><p className="eyebrow mb-4">Project enquiry</p><h2 className="text-2xl font-bold text-slate-900">Ready to tell us more?</h2><p className="mt-4 leading-relaxed text-slate-600">The button opens a draft in your email app. Review and send it when you are ready; this website does not submit it automatically.</p><a href={`${company.emailHref}?subject=${subject}&body=${body}`} className="button-dark mt-7">Open email draft <ArrowUpRight className="h-4 w-4" /></a><p className="mt-5 text-sm text-slate-600">You can also <Link className="text-link" href="/contact">view contact details</Link>.</p></div></div></section><Footer /></main>; }
