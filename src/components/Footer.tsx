"use client";
import React from "react";
import Link from "next/link";
import { Mail, Phone, MapPin } from "lucide-react";
import { company, navigation, services } from "@/lib/public-content";

export default function Footer() { return <footer className="bg-white pt-20 pb-10 border-t border-gray-100"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-16"><div><Link href="/" className="text-dark font-bold text-xl uppercase">{company.name}</Link><p className="text-grey text-sm font-medium italic mt-6">{company.tagline}</p></div><FooterList title="Quick Links" items={navigation.map(({ name, href }) => [name, href])} /><FooterList title="Services" items={services.slice(0, 6).map(([title,, href]) => [title, href])} /><div><h4 className="font-bold text-dark text-sm uppercase tracking-wider mb-6">Contact Us</h4><ul className="space-y-4 text-grey text-sm"><li className="flex gap-3"><Phone className="w-5 h-5 text-primary shrink-0" />{company.phone}</li><li className="flex gap-3"><Mail className="w-5 h-5 text-primary shrink-0" />{company.email}</li><li className="flex gap-3"><MapPin className="w-5 h-5 text-primary shrink-0" />{company.address.join(", ")}</li></ul></div></div><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 border-t border-gray-50 flex flex-col md:flex-row justify-between text-[10px] text-grey font-medium uppercase"><p>© 2026 {company.name}. All rights reserved.</p><div className="flex gap-6 mt-4 md:mt-0"><Link href="/privacy">Privacy Policy</Link><Link href="/terms">Terms & Conditions</Link></div></div></footer>; }
function FooterList({ title, items }: { title: string; items: readonly (readonly string[])[] }) { return <div><h4 className="font-bold text-dark text-sm uppercase tracking-wider mb-6">{title}</h4><ul className="space-y-4">{items.map(([name, href]) => <li key={href}><Link href={href} className="text-grey text-sm hover:text-primary">{name}</Link></li>)}</ul></div>; }
