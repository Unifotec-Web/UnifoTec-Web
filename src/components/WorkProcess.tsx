import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { deliverySteps } from "@/lib/public-content";
import Reveal from "@/components/motion/Reveal";

export default function WorkProcess() { return <section id="process" className="bg-[#edf1eb] py-16 sm:py-20 lg:py-24"><div className="section-shell"><Reveal className="mb-10 grid gap-5 lg:grid-cols-2 lg:items-end"><div><p className="eyebrow mb-4">Our process</p><h2 className="section-heading">A clear path from first conversation to launch.</h2></div><p className="max-w-xl text-slate-600 lg:ml-auto">Each step creates a useful point for discussion, feedback, and approval so the work stays connected to your goals.</p></Reveal><ol className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{deliverySteps.map(([title, description], index) => <li key={title} className="rounded-[1.4rem] border border-[#dbe5da] bg-white/80 p-6"><span className="mb-8 block text-sm font-bold text-[#285749]">{String(index + 1).padStart(2, "0")} / 06</span><h3 className="text-xl font-bold text-slate-900">{title}</h3><p className="mt-3 text-sm leading-relaxed text-slate-600">{description}</p></li>)}</ol><Link href="/process" className="text-link mt-8">See how we work <ArrowUpRight className="h-4 w-4" /></Link></div></section>; }
